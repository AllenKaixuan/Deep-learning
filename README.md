# Deep-learning
Nankai deep learning

1. FFN
   - usuage: python (--name) (--epochs) (--batchsize)
       - name
         FFN/Resmlp
         default = FFN
       - epochs
         default = 10
       - batchsizes
         default = 128
   - run.sh
2. CNN